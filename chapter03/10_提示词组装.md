# 10 提示词组装：告别面条代码，动态加载 AGENTS.md 与外挂 Skills

## 🎯 本节目标

设计一套模块化的 Prompt 组装系统，实现 System Prompt 的动态加载，彻底告别硬编码的"面条 Prompt"。

---

## 1. 面条 Prompt 的问题

```python
# ❌ 典型的面条代码 Prompt
SYSTEM_PROMPT = """
你是一个有用的 AI 助手。你可以帮助用户完成以下任务：
- 读取文件
- 修改代码
- 执行命令

规则1：在修改文件前先读取它
规则2：不要删除重要文件
规则3：...（还有 500 行规则）

你的工作目录是...
当前项目是...
可用工具是...
"""
```

问题：
1. **不可维护**：所有内容堆在一起，改一处要找很久
2. **不可复用**：相似项目需要复制粘贴整个 Prompt
3. **不可组合**：无法针对不同任务动态增减 Prompt 片段
4. **版本混乱**：不同 Agent 的 Prompt 各自维护，难以统一

---

## 2. 解决方案：文件驱动的 Prompt 系统

```
项目目录/
├── AGENTS.md              ← 核心 Agent 行为规范（项目级）
├── .workbuddy/
│   ├── IDENTITY.md        ← Agent 身份定义
│   ├── memory/
│   │   └── MEMORY.md      ← 长期记忆（见第 13 节）
│   └── skills/            ← 可插拔技能模块
│       ├── coding/
│       │   └── SKILL.md
│       └── devops/
│           └── SKILL.md
```

---

## 3. AGENTS.md 的结构规范

```markdown
<!-- AGENTS.md -->

# Agent 行为规范

## 身份
你是 OpenClaw，一个专注于代码开发的 Agent。

## 核心原则
- 先理解，再行动
- 最小变更，最大效果
- 明确报告每个操作的目的

## 工作流程

### 处理代码修改请求
1. 先用 list_dir 了解项目结构
2. 读取相关文件
3. 制定修改计划
4. 执行修改
5. 验证修改（如运行测试）

## 禁止行为
- 不得删除 .git 目录
- 不得修改 .env 文件（除非明确被要求）
- 不得在没有备份的情况下覆盖重要文件

## 工具使用指南
- 使用 `edit_file` 而不是 `write_file` 来修改现有文件
- 执行可能有副作用的命令前，先说明原因
```

---

## 4. Prompt Builder 实现

```python
# harness/prompt/builder.py

import os
from pathlib import Path
from typing import Optional
from dataclasses import dataclass, field


@dataclass
class PromptSection:
    """Prompt 的一个独立片段"""
    name: str
    content: str
    priority: int = 0       # 优先级（数字越小越靠前）
    enabled: bool = True


class PromptBuilder:
    """
    模块化 Prompt 构建器
    
    动态组装 System Prompt，支持：
    - 文件加载（AGENTS.md、IDENTITY.md 等）
    - Skills 外挂
    - 运行时上下文注入
    - 条件启用/禁用片段
    """
    
    def __init__(self, workspace: str = "."):
        self.workspace = Path(workspace)
        self._sections: list[PromptSection] = []
    
    # ── 加载来源 ─────────────────────────────────────────
    
    def load_agents_md(self) -> "PromptBuilder":
        """加载 AGENTS.md（项目级规范）"""
        path = self.workspace / "AGENTS.md"
        if path.exists():
            content = path.read_text(encoding="utf-8")
            self._sections.append(PromptSection(
                name="agents_md",
                content=content,
                priority=10,
            ))
        return self
    
    def load_identity(self) -> "PromptBuilder":
        """加载 .workbuddy/IDENTITY.md（Agent 身份）"""
        path = self.workspace / ".workbuddy" / "IDENTITY.md"
        if path.exists():
            content = path.read_text(encoding="utf-8")
            self._sections.append(PromptSection(
                name="identity",
                content=content,
                priority=5,  # 最高优先级
            ))
        return self
    
    def load_skills(self, skill_names: list[str] = None) -> "PromptBuilder":
        """
        加载 Skills（可插拔的专业能力模块）
        
        Args:
            skill_names: 指定要加载的 Skill 名称。None 表示加载所有已安装的 Skills
        """
        skills_dir = self.workspace / ".workbuddy" / "skills"
        if not skills_dir.exists():
            return self
        
        for skill_dir in sorted(skills_dir.iterdir()):
            if not skill_dir.is_dir():
                continue
            
            if skill_names and skill_dir.name not in skill_names:
                continue
            
            skill_md = skill_dir / "SKILL.md"
            if skill_md.exists():
                content = skill_md.read_text(encoding="utf-8")
                self._sections.append(PromptSection(
                    name=f"skill_{skill_dir.name}",
                    content=f"## Skill: {skill_dir.name}\n\n{content}",
                    priority=30,
                ))
        
        return self
    
    def load_memory(self) -> "PromptBuilder":
        """加载 Working Memory（见第 13 节）"""
        memory_path = self.workspace / ".workbuddy" / "memory" / "MEMORY.md"
        if memory_path.exists():
            content = memory_path.read_text(encoding="utf-8")
            if content.strip():
                self._sections.append(PromptSection(
                    name="memory",
                    content=f"## 持久记忆\n\n{content}",
                    priority=20,
                ))
        return self
    
    # ── 动态注入 ─────────────────────────────────────────
    
    def inject(
        self,
        name: str,
        content: str,
        priority: int = 50,
    ) -> "PromptBuilder":
        """动态注入 Prompt 片段（运行时上下文）"""
        self._sections.append(PromptSection(
            name=name,
            content=content,
            priority=priority,
        ))
        return self
    
    def inject_working_directory(self) -> "PromptBuilder":
        """注入当前工作目录信息"""
        return self.inject(
            name="working_directory",
            content=f"当前工作目录: `{self.workspace.absolute()}`",
            priority=25,
        )
    
    def inject_datetime(self) -> "PromptBuilder":
        """注入当前时间"""
        from datetime import datetime
        now = datetime.now().strftime("%Y-%m-%d %H:%M")
        return self.inject(
            name="datetime",
            content=f"当前时间: {now}",
            priority=25,
        )
    
    # ── 构建 ─────────────────────────────────────────────
    
    def build(self) -> str:
        """
        按优先级排序并拼接所有 Prompt 片段
        
        Returns:
            完整的 System Prompt 字符串
        """
        active_sections = [s for s in self._sections if s.enabled]
        sorted_sections = sorted(active_sections, key=lambda s: s.priority)
        
        parts = []
        for section in sorted_sections:
            content = section.content.strip()
            if content:
                parts.append(content)
        
        return "\n\n---\n\n".join(parts)
    
    def disable(self, name: str) -> "PromptBuilder":
        """禁用指定 Prompt 片段"""
        for section in self._sections:
            if section.name == name:
                section.enabled = False
        return self
```

---

## 5. 使用示例

```python
# 标准初始化流程
builder = PromptBuilder(workspace="/path/to/project")

system_prompt = (
    builder
    .load_identity()           # 加载身份定义
    .load_agents_md()          # 加载项目规范
    .load_memory()             # 加载长期记忆
    .load_skills(["coding"])   # 加载特定 Skills
    .inject_working_directory()
    .inject_datetime()
    .build()
)

# 传给 Main Loop
loop = MainLoop(
    provider=provider,
    tool_registry=registry,
    system_prompt=system_prompt,
)
```

---

## 6. Skills 文件结构示例

```markdown
<!-- .workbuddy/skills/coding/SKILL.md -->

---
name: coding
description: 代码开发专项能力
triggers:
  - 写代码
  - 修复 Bug
  - 代码审查
---

# Coding Skill

## 代码修改原则

1. **先读后改**：修改任何文件前，先完整读取它
2. **最小变更**：只修改必要的部分，不引入不必要的重构
3. **保持风格**：延续文件原有的代码风格和命名规范

## Python 开发规范

- 使用 f-string 而不是 .format()
- 异步函数使用 async/await
- 异常使用具体类型，不要裸 except

## 测试验证

修改代码后，建议运行：
\`\`\`bash
python -m pytest tests/ -v
\`\`\`
```

---

## 📝 本节小结

| 组件 | 职责 |
|---|---|
| `AGENTS.md` | 项目级 Agent 行为规范 |
| `IDENTITY.md` | Agent 身份与性格定义 |
| `SKILL.md` | 可插拔的专项能力模块 |
| `PromptBuilder` | 动态组装所有片段，按优先级排序 |

---

*上一节 → [09 飞书集成](../chapter02/09_飞书集成.md)*  
*下一节 → [11 会话管理：Session 隔离](./11_会话管理.md)*
